Alpine offers global state management through the `Alpine.store()` API.
## [Registering A Store](#registering-a-store)
You can either define an Alpine store inside of an `alpine:init` listener (in the case of including Alpine via a
